# mc-test JVM agents — shared Gradle properties.
# Java 17 source/target so the published core runs on the example's maven.compiler.release=17
# world, while the build itself runs on the installed JDK 21.
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.jvmargs=-Xmx1g
