# --- Node / TypeScript (packages/*) ---
node_modules/
dist/
build/
coverage/
*.tsbuildinfo
.npm/
.turbo/
.eslintcache

# --- JVM / Gradle (agents/*) ---
.gradle/
**/build/
*.class
out/
bin/
!**/src/**/bin/

# --- JVM / Maven (examples/regions/plugin) ---
# Build output (jar, classes, maven-status). The SUT jar is rebuilt with
# `mvn -f examples/regions/plugin/pom.xml package`; never commit it.
target/

# --- Minecraft run / provisioning artifacts ---
/artifacts/
/tmp/
run/
.mc-test/
mc-test-report/
server.jar
*.log
eula.txt
world/
world_nether/
world_the_end/
!examples/**/world-snapshot/

# --- Editors / OS ---
.idea/
*.iml
.vscode/
.DS_Store
Thumbs.db

# --- Secrets / local config ---
.env
.env.local
*.local.yml

# --- M3: JVM agents (Gradle) build output + scratch plan ---
agents/**/build/
agents/.gradle/
**/.gradle/
# Loom/NeoGradle/ForgeGradle scratch run dirs (e.g. `runGameTest`/`runs/`) — build output, never commit.
**/runs/
M3_PLAN.scratch.md
M4_PLAN.scratch.md

# --- Knowledge-graph tooling output (graphify) ---
graphify-out/
